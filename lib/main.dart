import 'package:flutter/material.dart';
import 'signaturewidget.dart';


const directoryName = 'pest';

void main() {
  runApp(
      MaterialApp(
        home: SignWidget(),
        debugShowCheckedModeBanner: false,
      )
  );
}
